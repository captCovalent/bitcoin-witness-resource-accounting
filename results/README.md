# Results

Generated results are ignored by default because historical transaction analyses can be large. A publishable result bundle should include small summary tables and plots, plus a manifest that makes the full output reproducible.

Do not publish raw witness payloads. Prefer transaction IDs, block identifiers, aggregate numeric measurements, model parameters, commands, and checksums.

